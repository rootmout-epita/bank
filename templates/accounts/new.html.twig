{% extends 'base.html.twig' %}

{% block title %}
    Nouveau compte
{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h3>Création d'un nouveau compte</h3>
        <br/>
        {{ form_start(form) }}
        <div class="">
            {{ form_row(form.name, {'label': 'Nom du compte','attr': {placeholder: ''}}) }}
        </div><br/>

        <div class="">
            {{ form_row(form.Balance, {'label': 'Balance initiale $ (CAD)','attr': {placeholder: ''}}) }}
        </div>
        <br/>

        <a href="{{ path('accounts') }}" class="btn btn-secondary">Retour</a>
        <button class="btn btn-primary">Appliquer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}

