{% extends 'base.html.twig' %}

{% block title %}

{% endblock %}

{% block body %}
    <div class="container mt-4">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Modifier</button>
    {{ form_end(form) }}
        <a href="{{ path('accounts') }}" class="btn btn-secondary">Retour</a>
    </div>
{% endblock %}

